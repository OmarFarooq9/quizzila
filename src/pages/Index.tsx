import QuizCard from "@/components/quiz/QuizCard";

const Index = () => {
  return (
    <main className="min-h-screen quiz-background flex items-center justify-center p-4 sm:p-8">
      <QuizCard />
    </main>
  );
};

export default Index;
